<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.aisw.wit.yaeipja.repo.CommunityMapper">
	
	<!-- 게시글 목록 조회 -->
	<select id="getCommunityList" resultType="net.aisw.wit.yaeipja.dto.CommunityDTO">
		SELECT 
			id, title, author, created_at, view_count, like_count
		FROM community 
		ORDER BY id DESC
	</select>
	
	<!-- 게시글 상세 조회 -->
	<select id="getCommunityById" parameterType="int" resultType="net.aisw.wit.yaeipja.dto.CommunityDTO">
		SELECT 
			id, title, content, author, created_at, updated_at, view_count, like_count
		FROM community 
		WHERE id = #{id}
	</select>
	
	<!-- 게시글 작성 -->
	<insert id="insertCommunity" parameterType="net.aisw.wit.yaeipja.dto.CommunityDTO">
		INSERT INTO community (
			title, content, author, created_at, updated_at, view_count, like_count
		) VALUES (
			#{title}, #{content}, #{author}, NOW(), NOW(), 0, 0
		)
	</insert>
	
	<!-- 게시글 수정 -->
	<update id="updateCommunity" parameterType="net.aisw.wit.yaeipja.dto.CommunityDTO">
		UPDATE community 
		SET 
			title = #{title},
			content = #{content},
			updated_at = NOW()
		WHERE id = #{id}
	</update>
	
	<!-- 게시글 삭제 -->
	<delete id="deleteCommunity" parameterType="int">
		DELETE FROM community WHERE id = #{id}
	</delete>
	
	<!-- 조회수 증가 -->
	<update id="increaseViewCount" parameterType="int">
		UPDATE community 
		SET view_count = view_count + 1 
		WHERE id = #{id}
	</update>
	
</mapper> 