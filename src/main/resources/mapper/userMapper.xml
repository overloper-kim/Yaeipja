<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.aisw.wit.yaeipja.repo.userMapper">
	<select id="getID" parameterType="net.aisw.wit.yaeipja.dto.userDTO" resultType="int">
		SELECT
			COUNT(*) as count
		FROM user
		WHERE
			id = #{id} 
	</select>
	
	<!-- 회원가입을 위한 insert 쿼리 -->
	<insert id="insertUser" parameterType="net.aisw.wit.yaeipja.dto.userDTO">
		INSERT INTO user (
			id, pw, nickname, birth_date, phone, address, sex, height, weight
		) VALUES (
			#{id}, #{pw}, #{nickname}, #{birth_date}, #{phone}, #{address}, #{sex}, #{height}, #{weight}
		)
	</insert>
</mapper>