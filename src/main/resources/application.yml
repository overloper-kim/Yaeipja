spring:
  application:
    name: yaeipja
  mvc:
    view:
      prefix: /view/
      suffix: .jsp

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yaeipja?characterEncoding=utf-8
    username: root
    password: project

mybatis:
  mapper-locations: classpath:mapper/*.xml